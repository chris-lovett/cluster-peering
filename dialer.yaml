apiVersion: consul.hashicorp.com/v1alpha1
kind: PeeringDialer
metadata:
  name: cluster-01
  namespace: consul
spec:
  peer:
    secret:
      name: "peering-token"
      key: "data"
      backend: "kubernetes"
